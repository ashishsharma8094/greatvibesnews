import React from "react";
import Pagination from "./Pagination";

const Footer = () => {
  return (
    <div
      style={{ marginTop: "1rem", padding: "1rem", backgroundColor: "#DBE2EF" }}
    >
      <Pagination />
    </div>
  );
};

export default Footer;
